<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type" />
    <meta name=viewport content='width=device-width,initial-scale=1' />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title><%= @title.present? ? "#{@title} | Padrino Admin" : "#{pat('login.title')} | Padrino Admin" %></title>
    <%= favicon_tag "favicon.ico" %>
    <%= stylesheet_link_tag "bootstrap", "application" %>

  </head>
  <body>
    <div class="jumbotron mx-auto bg-primary">
      <h1 class="display-4" style="text-align: center; color: white">Liftr</h1>
    </div>

    <% form_tag url(:auth, :create) do %>
        <%= {:error => 'danger', :warning => 'warning', :success => 'success', :notice => 'info'}.map { |type, class_name| flash_tag(type, :class => "alert alert-#{class_name} fade in", :bootstrap => true) }.join.html_safe %>
        <div class="form-group col-sm-7 mx-auto" style="padding-top: 20px">
          <label for="exampleInputEmail1">Email address</label>
          <%= email_field_tag :email, :id => :email, :value => params[:email], :autofocus => true, :class => 'form-control' %>
        </div>
        <div class="form-group col-sm-7 mx-auto">
          <label for="exampleInputPassword1">Password</label>
          <%= password_field_tag :password, :id => :password, :class => 'form-control' %>
        </div>
        <div class="form-check col-sm-7 mx-auto">
          <input type="checkbox" class="form-check-input" id="exampleCheck1">
          <label class="form-check-label" for="exampleCheck1">Check me out</label>
        </div>
        <div class="form-group col-sm-7 mx-auto" style="padding-top: 20px">
          <%= submit_tag(pat('login.sign_in'), :class => "btn btn-primary pull-right") %>
        </div>
    <% end %>
  </body>
</html>
